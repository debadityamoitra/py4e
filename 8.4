
fname = input("Enter file name: ")
fh = open(fname)
lst = list()
for line in fh:
    sentence=line.rstrip()
    word=sentence.split()
    for already in word:
        if already in lst:continue
        else:lst.append(already)
            
lst.sort()
print(lst)
